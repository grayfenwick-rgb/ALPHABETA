'use client';
import React from 'react';

export default function ARViewer() {
  return (
    <div style={{ padding: 16 }}>
      <h3>AR Viewer</h3>
      <p>This is the AR preview panel. Future features will include WebXR hooks or simulated AR placement tools.</p>
    </div>
  );
}
